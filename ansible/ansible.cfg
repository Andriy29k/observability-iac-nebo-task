[defaults]
inventory = inventory/hosts.ini
roles_path = roles
host_key_checking = False
retry_files_enabled = False

[inventory]
enable_plugins = ansible.builtin.host_list, ansible.builtin.ini, ansible.builtin.yaml

[privilege_escalation]
become = True
become_method = sudo
